<% provide(:title, 'Draft Articles') %>

<h1>Draft Articles</h1>
<%= will_paginate %>
<div class="articles drafts container">
  <% @articles.each do |article| %>
    <div class="article">
      <div class="row">
        <div class="col-md-9"> 
          <h1 class="title"> <%= link_to article.title, article.url %> </h1>
        </div>
        <div class="col-md-3">
            <%= link_to "Edit", edit_article_path(article.id), title: "Edit", class: "btn btn-primary" %>
            <%= link_to "Delete", article_path(article.id), method: :delete, data: { confirm: 'Are you sure?' }, title: "Delete", class: "btn btn-danger" %>
        </div>
      </div>
      <div class="row">
        <div class="col-md-3"> <span class="posted">Updated: <%= article.updated_at %> </span> </div>
        <div class="col-md-3"> <span class="posted">Created: <%= article.created_at %> </span> </div>
        <div class="col-md-3"> <span class="posted">Author: <%= article.user.name %> </span> </div>
        <div class="col-md-3"> <%= render 'articles/tag_list', tag_list: article.tag_list %> </div>
      </div>
    </div>
  <% end %>
</div>

<%= will_paginate %>
